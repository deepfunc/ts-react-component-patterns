import React, { SFC } from 'react';
import Button from './stateless/Button';
import CounterButton from './stateful/CounterButton';
import ButtonWithDefaultProps from './defaultProps/ButtonWithDefaultProps';
import Toggleable from './renderCallback/Toggleable';
import Panel from './renderCallback/Panel';

const Test: SFC = () => {
    return (
        <div>Test</div>
    );
};